services:
  - type: web
    name: f1-race-analytics
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/streamlit_app.py --server.address=0.0.0.0 --server.port=$PORT --server.headless=true
    envVars:
      - key: "postgresql://neondb_owner:npg_JrahOB1D5lxo@ep-flat-butterfly-aetczgxx-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
        sync: false
